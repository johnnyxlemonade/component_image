name: PHPUnit Test Suite

on:
  push:
    branches: [modern, master]
    paths:
      - 'src/**'
      - 'tests/**'
      - 'composer.json'
      - 'phpunit.xml.dist'
      - '.github/workflows/phpunit.yml'

  pull_request:
    paths:
      - 'src/**'
      - 'tests/**'
      - 'composer.json'
      - 'phpunit.xml.dist'
      - '.github/workflows/phpunit.yml'

  workflow_dispatch:

concurrency:
  group: phpunit-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  phpunit:
    name: PHPUnit (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php: ['8.5', '8.4', '8.3', '8.2', '8.1']

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate repository structure
        run: |
          set -euo pipefail

          if [ ! -f composer.json ]; then
            echo "::error::composer.json is missing. Repository is in invalid state."
            exit 1
          fi

          if [ ! -f phpunit.xml.dist ]; then
            echo "::error::phpunit.xml.dist is missing. Repository is in invalid state."
            exit 1
          fi

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer:v2
          extensions: gd, mbstring, fileinfo

      - name: Validate composer.json
        run: composer validate --strict

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Composer packages
        uses: actions/cache@v5
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: composer-${{ runner.os }}-php-${{ matrix.php }}-phpunit-${{ hashFiles('composer.json', 'phpunit.xml.dist') }}
          restore-keys: composer-${{ runner.os }}-php-${{ matrix.php }}-phpunit-

      - name: Install dependencies
        run: composer update --no-interaction --prefer-dist --no-progress

      - name: Run PHPUnit
        run: composer test:ci
